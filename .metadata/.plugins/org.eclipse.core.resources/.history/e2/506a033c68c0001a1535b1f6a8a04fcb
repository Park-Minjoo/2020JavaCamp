package image;

// 돌이 4개냐?
// 2개를 둔다 
public class fourBlock {
	private static int fourC = 0;
	
	if(fourC == 1) { // 흑돌의 연결이 4개인 경우 
		//흰돌을 양쪽에 둔다. 
     }else if(Winner == 2) { // 흰돌의 연결이 4개인 경우 
    	 //검은돌을 양쪽에 둔다. 
     }
	
	public static void set(int four) {
	    fourC = four;
	 }
	
}

